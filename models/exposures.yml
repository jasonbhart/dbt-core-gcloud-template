version: 2

exposures:
  - name: example_dashboard
    type: dashboard
    maturity: low
    owner:
      name: Data Team
      email: data@example.com
    depends_on:
      - ref('fct_example')
    description: |
      Example dashboard exposure depending on fct_example.
    tags: [example, docs]
    meta:
      env: "{{ var('DBT_ENV_NAME') }}"

